drawBoard()
drawInitial()
// var previewTurns = calculatePreviewTurnsForThisRound([])
// drawPreviewTurns(previewTurns.slice(0, -1))
// clearPreviewTurns()
//
// moveSnakeHeadForThisRound(previewTurns.slice(0,-1))
// moveSnakeHeadToGoal(previewTurns.slice(-1)[0])
//
// var previewTurns = calculatePreviewTurnsForThisRound([])
// drawPreviewTurns(previewTurns.slice(0, -1))
// clearPreviewTurns()
// moveSnakeHeadForThisRound(previewTurns.slice(0,-1))
// moveSnakeHeadToGoal(previewTurns.slice(-1)[0])